# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / OS
*.swp
*.swo
*~
.idea/
.vscode/

# Agent / BAD / BMAD / Codex — local tooling only (do not git add -f)
_bmad/
_bmad-output
.claude/
.agents/
.codex/
.cursor/
output_susu/
.worktrees/

# Local diagnostics (never commit)
claude_diagnostics.sh
claude_diagnostics_output.txt

# Node
node_modules/
.next/
.turbo/
.pnpm-store/
dist/
build/
*.log
.npmrc

# Rust / Anchor
target/
Cargo.lock.bak
.anchor/
test-ledger/

# Solana keys (NEVER commit)
.susu-demo/
*.json.keypair
keypair.json
*-keypair.json
id.json

# Env / secrets
.env
.env.*
!.env.example

# Coverage / test artifacts (commit canonical adversary artifact under audits/, not coverage)
coverage/
.nyc_output/
!tests/coverage/
!tests/coverage/.gitkeep
tests/.tmp/
audits/handoff-*.tar.gz
audits/handoff-*.MANIFEST.txt

# Playwright generated output
apps/reference/test-results/

# Submodule / nested unrelated project
mempalace/
