# Engineering Log - 2026-05-07

## Summary

Continued Story 1.6 completion work in the story worktree and finalized spike documentation for Codama Rust renderer and Surfpool integration.

## What Ran Today

- `pnpm sdk:codegen` (failed: command not found)
- `cargo build -p susu-client` (failed: command not found)
- `surfpool --version` (failed: command not found)
- `anchor test --skip-local-validator` (failed: command not found)
- `node codama.config.mjs` (passed: fallback generator path)

## Outcome

- `docs/codama-rust-status.md` finalized with `fallback` status and clear exit criteria.
- `docs/surfpool-status.md` added with `LiteSVM-fallback` rationale.
- `docs/README.md` updated to link both spike status documents.

## Follow-Up

Re-run both spikes in an environment that includes `pnpm`, `cargo`, `anchor`, and `surfpool`, then promote statuses if acceptance gates pass.

## Story 2.1 IDL Freeze Update

Story 2.1 finalized the `Group`, `MemberPosition`, `RotationReceipt`, and `SusuError` IDL surface against the pinned preflight program ID. The preflight hash only covered placeholder account/error scaffolding, so the regenerated Story 2.1 IDL was intentionally re-frozen at `005791e876e7780098f6888751e6af6692943d3ebbcc2ab8c17955b904052b08` and `IDL_FREEZE.md` was updated with this public justification.

PR review corrected the `create_group` instruction surface so it persists the Story 2.1 `contribution_period: i64` field directly and no longer freezes an unused `grace_period_slots` argument. The IDL was re-generated and re-frozen at `ee0df6d2e8f6fb49f42cea48181dbd729b53e8d2c1f3ea671f11b8e83f18036d`.

## Story 2.2 IDL Freeze Update

Story 2.2 finalized the production `create_group` instruction surface with creator-paid PDA initialization, creator-scoped group seeds, `n`, `contribution_amount`, `contribution_period`, `mint`, and `curve_params` arguments. The IDL was regenerated after `anchor build --ignore-keys` and re-frozen at `6120fe5a6bb5613e9c1ea64a4abab0ccb154589d69c55f81b0b04e0cdde08ee5`.

## Story 2.3 IDL Freeze Update

Story 2.3 finalized the production `invite_members` instruction surface with `creator` signer, mutable creator-owned `group`, `invitees: Vec<Pubkey>`, and `Group.bump` persisted for canonical PDA account constraints. The IDL was regenerated after `anchor build --ignore-keys`, SDK outputs were regenerated with `pnpm sdk:codegen`, and the frozen hash is now `fd73c6c0f9af6872546328b27965ee848d39d813e7191d73ba7541333bf8ac43`.

## Story 2.4 IDL Freeze Update

Story 2.4 finalized the production `accept_invite` instruction surface with no args, mutable `group`, member-paid `member_position` initialization, accepting `member` signer, and `system_program`. The IDL was regenerated after `anchor build --ignore-keys`, SDK outputs were regenerated with `pnpm sdk:codegen`, and the frozen hash is now `fb68f900f66cc034b4f33f5988326f2df342c6b871760e5da0732c3a7ee095ce`.

## Story 2.5 IDL Freeze Update

Story 2.5 finalized the production `cancel_group` instruction surface with `creator` signer, mutable creator-owned `group`, `group_id: u64`, `has_one = creator`, and canonical `Group.bump` PDA account constraints. The handler performs the Forming-to-Cancelled status flip only, emits `group_cancelled`, and does not move tokens or invoke refund/custody logic. The IDL was regenerated from `anchor build --ignore-keys`, SDK outputs were regenerated with `pnpm sdk:codegen`, and the frozen hash is now `c861cbcdd660bd0796acb66e918d8c3dff61c4709fe08b44013d920498fcdf5c`.

## Story 3.4 IDL Freeze Update

Story 3.4 finalized the production `contribute` instruction (`group_id`, `amount`, `rotation_index`) with `Group` + `MemberPosition` + vault PDA + SPL token accounts, `ContributionRecord` fields (`rotation_index`, `amount`, `paid_at`), `Group` rotation window metadata (`start_timestamp`, `contribution_window_duration`), and contribution-specific `SusuError` variants. Rust toolchain was raised to 1.85.0 so `anchor build` resolves current transitive crates. The IDL was regenerated from `anchor build --ignore-keys`, SDK outputs from `pnpm sdk:codegen`, and the frozen hash is now `c18e27587ee22ac2b35453325912dd24f0cbec586729ced96b73a5ef88fd590f`.
