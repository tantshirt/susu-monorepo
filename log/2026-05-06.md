# Engineering Log - 2026-05-06 (T+0)

## Summary

Epic 1 foundation work landed across scaffold, Anchor shell structure, codegen scaffolding, CI guardrails, and contribution/governance docs. Story 1.6 was used to close the daily log discipline requirement and record spike outcomes transparently.

## Per-Story Notes

### 1.1 - Monorepo Skeleton

Initialized monorepo structure for apps, programs, SDKs, tests, docs, and operations folders with baseline configs.

### 1.2 - Anchor Program Shell

Added the `susu` program shell with instruction placeholders, state modules, seed constants, and error scaffolding for the 9 core instruction paths.

### 1.3 - Codama Codegen

Added generated TS/Rust SDK surfaces and fallback generation pipeline. Full renderer verification was blocked by missing `pnpm` and `cargo` in this environment.

### 1.4 - CI Scaffold

Added CI workflow shell and policy scripts (`check-idl-hash`, `check-patterns`, `check-sdk-parity`, `check-i18n-parity`, `check-fincen-posture`) with honest pass/fail reporting based on local tool availability.

### 1.5 - Contribution and Governance Docs

Added `CONTRIBUTING`, `CONTRIBUTING-TRANSLATIONS`, CODEOWNERS, issue/PR templates, and Dependabot configuration.

### 1.6 - Daily Log + Day-1 Spikes

Recorded the Day-1 spike outcomes and documented fallback decisions:

- Codama Rust renderer: `fallback` (no `pnpm`/`cargo`/`anchor` in this environment)
- Surfpool integration: `LiteSVM-fallback` (no `surfpool` or `anchor` binaries)

## Decisions

- Keep Codama on fallback until both `pnpm sdk:codegen` and `cargo build -p susu-client` succeed in a full toolchain environment.
- Use LiteSVM fallback path for Epic 2 local test strategy until Surfpool integration can be validated with a compatible toolchain.

## Blockers Carried To T+1

- `pnpm` unavailable
- `cargo` unavailable
- `anchor` unavailable
- `surfpool` unavailable

## Plan For Tomorrow

- Continue Epic 2 with toolchain-aware execution.
- Re-run Codama and Surfpool spikes on a fully provisioned machine and update status docs if outcomes improve.
