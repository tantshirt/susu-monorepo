# Engineering Log - 2026-05-08

## Story 4.2 IDL Freeze Update

Story 4.2 finalized the production `claim_payout` instruction surface with recipient signer, recipient `MemberPosition`, recipient token account, group vault, mint, `RotationReceipt` initialization, SPL Token program, system program, and Clock sysvar accounts.

The `RotationReceipt` account now stores its PDA bump alongside `group`, `rotation_index`, `amount`, `recipient`, and `claimed_at`. The IDL also exposes payout guard errors `ArithmeticOverflow`, `NotRotationRecipient`, and `RotationNotClosed`.

The IDL was regenerated from `anchor build --ignore-keys`, SDK error stubs from `pnpm sdk:codegen`, and the frozen hash is now `ca4cfce9e3bfc6d6e266c685bd4b968750593e0e9d369be847264bc8fcf09e72`.

