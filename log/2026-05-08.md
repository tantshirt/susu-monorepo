# Engineering Log - 2026-05-08

## Story 4.2 IDL Freeze Update

Story 4.2 finalized the production `claim_payout` instruction surface with recipient signer, recipient `MemberPosition`, recipient token account, group vault, mint, `RotationReceipt` initialization, SPL Token program, system program, and Clock sysvar accounts.

The `RotationReceipt` account now stores its PDA bump alongside `group`, `rotation_index`, `amount`, `recipient`, and `claimed_at`. The IDL also exposes payout guard errors `ArithmeticOverflow`, `NotRotationRecipient`, and `RotationNotClosed`.

The IDL was regenerated from `anchor build --ignore-keys`, SDK error stubs from `pnpm sdk:codegen`, and the frozen hash is now `ca4cfce9e3bfc6d6e266c685bd4b968750593e0e9d369be847264bc8fcf09e72`.


## Story 4.4 IDL Freeze Update

Story 4.4 finalized the strict pre-deadline `claim_payout` guard by adding `SusuError::ContributionPeriodOpen` for recipient claims submitted at or before a rotation close timestamp. The handler keeps checked deadline arithmetic and strict `Clock.unix_timestamp > deadline` semantics.

The IDL now exposes `ContributionPeriodOpen`; SDK error stubs were regenerated with `pnpm sdk:codegen`, and the frozen hash is now `524dcc8ea7e572e0c651b9c35a8f382a0ff20a2e0b0f4ba272e343bec483ff4b`.

## Story 4.6 IDL Freeze Update

Story 4.6 added the signed `complete_group(group_id)` transition for the Epic 4 lifecycle capstone. The instruction verifies every canonical `RotationReceipt` PDA for the group before moving `Group.status` from `Active` to `Completed`, enabling the terminal collateral-withdrawal phase without any external timing service.

The IDL was regenerated from `anchor build --ignore-keys`, SDK outputs from `pnpm sdk:codegen`, and the frozen hash is now `a349f5aea7e6e2427e58f2bb50c51a0b2b9b407068955b6ea56ecde45f6f08ea`.
