// DO NOT EDIT — generated by codama
export const GROUP_SEED = 'group';
export const GROUP_SEED_BYTES = new TextEncoder().encode(GROUP_SEED);

export const MEMBER_SEED = 'member';
export const MEMBER_SEED_BYTES = new TextEncoder().encode(MEMBER_SEED);

export const VAULT_SEED = 'vault';
export const VAULT_SEED_BYTES = new TextEncoder().encode(VAULT_SEED);

export const ROTATION_SEED = 'rotation';
export const ROTATION_SEED_BYTES = new TextEncoder().encode(ROTATION_SEED);
