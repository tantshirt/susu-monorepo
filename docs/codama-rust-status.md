# Codama Rust Renderer Status

- Story: `1.3-codama-codegen`
- Date: `2026-05-07`
- Status: `fallback`

## Current State

The environment does not currently provide `pnpm`, `cargo`, or `anchor`, and Codama packages are not installed. Because of that, full Codama Rust renderer execution could not be verified in this run.

To keep Story 1.3 unblocked, `pnpm sdk:codegen` now runs `codama.config.mjs`, which generates deterministic TS/Rust scaffold output from `programs/susu/idl/susu.json` and preserves the required banner comment (`DO NOT EDIT — generated by codama`) in all generated files.

## What Is Covered

- Generated TS tree at `sdk/ts/src/generated/` with instruction builders, account decoders, error enum, and seed constants.
- Generated Rust tree at `sdk/rust/src/generated/` with instruction enum/functions, account structs, error enum, and seed constants.
- Naming parity enforced in generated artifacts:
  - TS instructions: `camelCase` (e.g., `createGroup`)
  - Rust instructions: `snake_case` (e.g., `create_group`)

## What Is Not Yet Verified

- Real Codama invocation using `@codama/nodes-from-anchor`, `@codama/renderers-js`, and `@codama/renderers-rust`.
- `cargo build -p susu-client` against renderer-produced Rust output.
- Anchor-built/frozen IDL provenance from `anchor build`.

## Fallback Plan (PRD Cut #4)

Until renderer/toolchain coverage is confirmed, keep using the generated thin Rust client surface in `sdk/rust/src/generated/` and layer hand-rolled helper APIs separately (later stories). When tooling is available, run:

1. `pnpm install`
2. `pnpm sdk:codegen`
3. `cargo build -p susu-client`

If renderer output is incomplete, keep this fallback status and document missing renderer areas before promoting status to `partial` or `working`.
