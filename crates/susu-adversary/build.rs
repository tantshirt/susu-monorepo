fn main() {
    // Kept as a Story 5.2 source-layout sentinel. Story 5.4 derives report
    // metadata from explicit CLI input, so this script intentionally emits no
    // build-time environment values.
}
